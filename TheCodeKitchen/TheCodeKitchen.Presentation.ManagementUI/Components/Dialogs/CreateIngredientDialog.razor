<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h4">Create Ingredient</MudText>
    </TitleContent>
    <DialogContent>
        <MudForm Model="@Model" Validation="@validator.ValidateValue" @ref="Form">
            <MudTextField Label="Name"
                          @bind-Value="Model.Name"
                          For="@(() => Model.Name)"
                          Required
                          Variant="Variant.Outlined"/>
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton OnClick="Submit" Color="Color.Primary" Variant="Variant.Filled" Disabled="Creating">OK</MudButton>
    </DialogActions>
</MudDialog>